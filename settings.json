{
  "permissions": {
    "deny": [
      "Bash(git config:*)",
      "Bash(brew install:*)",
      "Bash(chmod 777:*)",
      "Bash(rm -rf /*)",
      "Bash(gh repo delete:*)"
    ],
    "defaultMode": "bypassPermissions"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/scripts/deny-check.sh"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); current_dir=$(echo \"$input\" | jq -r '.workspace.current_dir'); if [ -d \"$current_dir/.git\" ] || git -C \"$current_dir\" rev-parse --git-dir > /dev/null 2>&1; then branch=$(git -C \"$current_dir\" branch --show-current 2>/dev/null || git -C \"$current_dir\" rev-parse --short HEAD 2>/dev/null || echo 'unknown'); printf \"\\033[2m%s on \\033[0;36m%s\\033[0;2m in %s\\033[0m\" \"$(echo \"$input\" | jq -r '.model.display_name')\" \"$branch\" \"$(basename \"$current_dir\")\"; else printf \"\\033[2m%s in %s\\033[0m\" \"$(echo \"$input\" | jq -r '.model.display_name')\" \"$(basename \"$current_dir\")\"; fi"
  },
  "alwaysThinkingEnabled": true
}
