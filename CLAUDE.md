日本語で説明してください。

## Git Workflow Rules

plan modeから実行モードに切り替える際、現在のブランチが main, develop, staging のいずれかである場合:
1. 作業開始前に新しいブランチを作成する（例: feat/xxx, fix/xxx）
2. 作業完了後にPRを作成する（/create-branch-pr または /pr コマンドを使用）

## Agent Team Rules

エージェントチームは**既定では編成しない**。委譲は、独立して並行実行できる
大きめの作業に対してだけ行う。数回のツール実行で自分で終わる作業は委譲しない。
1体で足りるなら1体にし、起動数は低く保つ。

### 委譲してよい役割

| 役割 | 使いどころ |
|------|------|
| **Project Manager (Tech Lead)** | 複数トラックに分かれる大きめのタスクで、分割・依存関係・進捗を管理するとき |
| **実装者** | 独立した複数モジュール/複数ファイルを並行実装できるとき。1ファイルの変更では使わない |
| **テスト担当** | 実装とは別に、まとまった量のテストを書くとき |
| **レビュワー** | 差分が大きい、またはセキュリティ・パフォーマンス上のリスクが高いとき |
| **悪魔の代弁者** | 設計フェーズで方針・アーキテクチャを決めるとき（後戻りコストが高い判断に限る） |

### 検証目的では委譲しない
自分の作業の検証・ダブルチェック・「念のためのQA」にサブエージェントを使わない。
Claude Opus 5 は指示なしで自己検証するため、検証役を常設すると作業が重複し、
トークンと時間を浪費するだけで品質は上がらない。
同じ理由で、プロンプトや CLAUDE.md に「最後に検証ステップを入れる」
「サブエージェントに検証させる」といった指示を書かない。
※ CI で実行する実際のテストはこれに含まれない。必須のまま。

出典: https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5